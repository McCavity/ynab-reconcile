# YNAB API Konfiguration
# Kopiere diese Datei zu ".env" und trage deinen Token ein
# Den Token findest du in YNAB unter: Account Settings → Developer Settings → Personal Access Tokens

YNAB_API_TOKEN=dein_token_hier_eintragen
